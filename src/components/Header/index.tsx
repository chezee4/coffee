import { FC } from "react";
import Navigation from "../Navigation";
const Header: FC = () => {
  return (
    <header className="  text-white max-w-[1500px] m-auto">
      <Navigation />
    </header>
  );
};

export default Header;
